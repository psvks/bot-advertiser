local loader = loadstring(readfile('robloxSetupApi/loader.lua'))


--[[
    Every function you are going to use will be here. Type above. Otherwise it will not work

    Docummentation:
        loader.client.say_and_rejoin()
        loader.client.say()
        loader.client.rejoin()
        loader.getRamdomMath()

    Common errors:
        [ERR]: Is 'value: string' empty? Please assing atleast a number. The value was set to ten for default settings. -- Self explanatory.

    Configs can be found inside loader.lua -- If you don't uderstand you could see the video.
]] 


